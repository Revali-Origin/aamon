# プロジェクト名

## ✨ Features（機能）
- スラッシュコマンド対応
- コマンド実行権限対応
- Guild入退会通知
- BlackList管理
- メンテナンス報知(ESO/FF14/Fo76)
- サーバー管理(起動/停止/更新)
- サーバー監視(状態/人数)
- 販売物リスト通知(ESO)
- ダイス

## 📦 Requirements（前提）

## 🚀 Setup（セットアップ）
```bash
```

## ▶️ Run（実行）
```bash
```

## ⚙️ Environment Variables（環境変数）

## 📁 Directory Structure（フォルダ構成）
```text
Aamon/
├─ README.md                                # 本ファイル
├─ .env                                     # 本番用の秘密設定（トークン/DB等）※Gitに上げない
├─ .env.example                             # 共有用の見本ファイル（ダミー値を入れる）
├─ .gitignore                               # Gitで無視するもの（node_modulesや.envなど）
├─ package.json                             # 依存ライブラリと起動コマンドの定義
├─ tsconfig.json                            # TypeScriptの設定（ESバージョン等）
├─ eslint.config.js                         # コードの書き方チェック（任意だが推奨）
├─ prettier.config.cjs                      # 自動整形ルール（任意だが推奨）
│
├─ prisma/                                  # ★DB（MariaDB）の設計図と履歴
│  ├─ schema.prisma                         # テーブル定義（Guild/Subscription/Blacklistなど）
│  └─ migrations/                           # マイグレーションの履歴（自動生成）
│
├─ src/                                     # ★ここにBOTの本体コードを置く
│  ├─ index.ts                              # アプリの入口：起動/ジョブ開始/終了処理
│  ├─ bot.ts                                # Discord Clientの作成・ログイン・ハンドラ読み込み
│  │
│  ├─ config/                               # 設定周り（環境変数や固定値）
│  │  ├─ env.ts                             # .envを読み込んで型チェック（必須値の検証）
│  │  ├─ constants.ts                       # 変わらない定数（ゲーム名/Embed色など）
│  │  └─ services.whitelist.ts              # systemdで操作を許可するサービス名の一覧（安全対策）
│  │
│  ├─ infrastructure/                       # 外部とのやり取り（DB/ログ/ジョブ/API/systemd）
│  │  ├─ logger.ts                          # ログ出力（pino/winston等、JSON形式推奨）
│  │  ├─ systemd.ts                         # Ubuntuのsystemctlを安全に呼ぶラッパー
│  │  │
│  │  ├─ db/
│  │  │  ├─ prismaClient.ts                 # Prismaの接続クライアント（全体で使い回す）
│  │  │  └─ seed.ts                         # 開発用の初期データ投入（任意）
│  │  │
│  │  ├─ jobs/
│  │  │  ├─ schedulers.ts                   # 定期実行（cron）をまとめる入口
│  │  │  ├─ job.status.ts                   # サーバーステータス取得→差分があれば通知
│  │  │  ├─ job.maintenance.ts              # メンテ期間の取得→予告/開始/終了を通知
│  │  │  └─ job.eso.vendor.ts               # ESO週末家具/アバダロの取得→保存→更新通知
│  │  │
│  │  └─ providers/                         # 外部サイト/APIの取得実装（入替え前提）
│  │     ├─ types.ts                        # 取得インターフェースの型定義（共通IF）
│  │     ├─ eso.provider.ts                 # ESOの販売/状態/メンテ情報の取得
│  │     ├─ ff14.provider.ts                # FF14の状態/メンテ取得
│  │     └─ fo76.provider.ts                # Fo76の状態/メンテ取得
│  │
│  ├─ domain/                               # アプリの「概念・型」（DBやDiscordに依存しない）
│  │  ├─ entities/                          # エンティティ（例：VendorItem 等）※必要な分だけ
│  │  ├─ repositories/                      # リポジトリの「インターフェース」（約束だけ書く）
│  │  └─ services/                          # ドメインロジックのIF（外部I/Oなしの純粋な処理）
│  │
│  ├─ data-access/                          # DBアクセスの実装（Prismaでrepositoriesを実装）
│  │  ├─ GuildRepoPrisma.ts                 # GuildテーブルのCRUD実装
│  │  ├─ SubscriptionRepoPrisma.ts
│  │  ├─ BlacklistRepoPrisma.ts
│  │  ├─ VendorRepoPrisma.ts
│  │  └─ StatusRepoPrisma.ts
│  │
│  ├─ application/                          # ★機能の本体（ユースケース）ここを一番触る
│  │  ├─ status/
│  │  │  ├─ checkServerStatuses.ts          # 取得→保存→前回と比較
│  │  │  └─ notifyStatusChange.ts           # 変化があれば対象ギルドへ通知
│  │  │
│  │  ├─ maintenance/
│  │  │  ├─ fetchMaintenances.ts            # メンテ期間の取得→保存
│  │  │  └─ scheduleMaintenanceNotices.ts   # 予告/開始/終了の通知スケジュール
│  │  │
│  │  ├─ eso/
│  │  │  ├─ fetchVendor.ts                  # 週末家具/アバダロ取得→保存（重複防止）
│  │  │  └─ listVendor.ts                   # Embed表示用に見やすく整形
│  │  │
│  │  ├─ guild/
│  │  │  ├─ ensureGuild.ts                  # ギルド参加時に初期レコード作成
│  │  │  └─ blacklistCheck.ts               # 参加時にBL照合→一致ならキック
│  │  │
│  │  └─ server/
│  │     ├─ start.ts                        # systemd start（許可リスト内のみ）
│  │     ├─ stop.ts                         # systemd stop
│  │     └─ updateArk.ts                    # Arkだけ更新コマンドを許可
│  │
│  ├─ interface/
│  │  └─ discord/                           # Discordとの接点（コマンド/イベント/Embed）
│  │     ├─ commands/
│  │     │  ├─ index.ts                     # コマンドを自動読み込みして登録
│  │     │  ├─ eso.vendor.ts                # /eso vendor [luxury|golden]
│  │     │  ├─ status.subscribe.ts          # /status subscribe
│  │     │  ├─ status.unsubscribe.ts        # /status unsubscribe
│  │     │  ├─ maintenance.subscribe.ts     # /maintenance subscribe
│  │     │  ├─ maintenance.list.ts          # /maintenance list
│  │     │  ├─ server.start.ts              # /server start
│  │     │  ├─ server.stop.ts               # /server stop
│  │     │  ├─ server.update.ts             # /server update（Arkのみ有効）
│  │     │  ├─ rand000.ts                   # /rand000（000〜999を返す）
│  │     │  ├─ admin.setNotifyChannel.ts    # 既定通知先を設定
│  │     │  ├─ admin.blacklist.add.ts       # BLに追加
│  │     │  └─ admin.blacklist.remove.ts    # BLから削除
│  │     │
│  │     ├─ events/
│  │     │  ├─ ready.ts                     # 起動完了時の処理（ログなど）
│  │     │  ├─ interactionCreate.ts         # 受けたスラコマを各コマンドへ振り分け
│  │     │  ├─ guildMemberAdd.ts            # 参加通知＋BL照合→キック
│  │     │  └─ guildMemberRemove.ts         # 退会通知
│  │     │
│  │     └─ utils/
│  │        ├─ embeds.ts                    # Embedの見た目を統一（色/フッター等）
│  │        └─ replyHelpers.ts              # 返信やチャンネル投稿の共通関数
│  │
│  ├─ utils/                                # 小さな便利関数（どこからでも使える）
│  │  ├─ result.ts                          # Result/Option 風ヘルパ（try/catchを減らす）
│  │  ├─ time.ts                            # 時刻/タイムゾーンの整形（JST/UTC変換など）
│  │  └─ string.ts                          # 文字列ヘルパ（000パディング等）
│  │
│  └─ types/
│     └─ global.d.ts                        # 型の拡張（ProcessEnvのキー追加等）
│
├─ scripts/                                 # 手動実行の便利スクリプト
│  ├─ register-commands.ts                  # スラコマ登録/更新（開発はギルド、本番はグローバル）
│  ├─ migrate.ts                            # DBマイグレーション実行まとめ
│  └─ healthcheck.ts                        # 死活監視（起動/DB接続OKなら0終了）
│
├─ deploy/                                  # サーバ運用用の設定ファイル
│  ├─ pm2.config.cjs                        # pm2での常駐設定
│  ├─ systemd.service                       # systemdでの自動起動設定テンプレ
│  └─ nginx.conf.example                    # （必要なら）Web管理用の参考設定
│
├─ tests/                                   # テストコード（あとで増やせばOK）
│  ├─ unit/                                 # 小さな部品のテスト
│  └─ integration/                          # 複数機能を合わせたテスト
│
├─ var/                                     # 生成物（Gitに上げない）
├─ logs/                                    # 実行ログ（logrotate推奨）
└─ tmp/                                     # 一時ファイル/キャッシュ
```

## 🧪 Development（開発）
```bash
```

## 📄 License（規約）
